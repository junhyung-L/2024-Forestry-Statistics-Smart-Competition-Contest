# 산림 작물 적합지 분석: 통계 검정과 작물별 모델을 연결한 재배 의사결정 도구

[English](PORTFOLIO.md) | [한국어](PORTFOLIO.ko.md)

## 프로젝트 개요

예비 임업인이 작물을 고를 때 토양·기후·생산량 자료는 흩어져 있고, 기존 적지 데이터는 비전공자가 읽기 어렵다. 이 프로젝트는 작물별 생산량과 관련된 환경 조건을 분석하고, 그 조건에 맞는 지역을 지도에서 찾을 수 있게 만드는 것을 목표로 했다. 2024 산림임업 통계 스마트 활용 경진대회에서 대상(산림청장상, 1위)을 수상했다.

## 분석 설계

토성, 토양 깊이, 유효수분량 같은 토양 변수와 기온·강수량·상대습도 같은 기후 변수, 작물별 생산량을 하나의 분석 테이블로 결합했다. Levene 검정으로 분산 조건을 보고, ANOVA와 Chi-square 검정으로 작물별 후보 변수를 좁혔다. 생산량이 0이 아닌 관측치에서는 분위수 구간을 사용해 생산량 분포를 나눴다.

## 작물별 모델 선택

하나의 알고리즘을 모든 작물에 강요하지 않았다. 평균 생산량 이상 여부를 이진화한 분류 문제에서는 SVM·Random Forest·Logistic Regression을 비교했고, 생산량 자체는 회귀 경로로 분리했다. 보고서는 마에 SVM, 복분자딸기·생표고·더덕·오갈피에 Random Forest, 밤·도라지에 Logistic Regression이 상대적으로 적합했다고 기록한다. 결과 조건은 지역 기후·토양 조건과 매칭해 추천 후보를 추출하는 데 썼다.

## 결과와 해석

보관된 ROC 그림에는 밤 AUC 0.94, 마 AUC 0.91, 복분자 AUC 0.84가 표시돼 있다. 이 프로젝트의 핵심은 작물마다 필요한 변수와 적합한 모델이 다르다는 사실을 지도·대시보드 구상으로 연결한 데 있다.

## 한계와 다음 단계

원본 데이터와 당시 분할·환경이 완전하게 남아 있지 않다. ROC 수치는 보관된 프로젝트 산출물로 제시하며 최신 재실행 벤치마크로 단정하지 않는다. 이후에는 공간·시간 분할과 실제 재배 결과 대조가 필요하다.

## 근거

- 최종 보고서: [`reports/산림자원_활용을_용이하게_하는_비즈니스모델_개발_및_웹페이지_개발(엄홍길동).pdf`](reports/산림자원_활용을_용이하게_하는_비즈니스모델_개발_및_웹페이지_개발%28엄홍길동%29.pdf)
- ROC 산출물: [`images/`](images/)
- 프로젝트 리뷰: [`docs/PROJECT_REVIEW.md`](docs/PROJECT_REVIEW.md)

## 분석을 의사결정으로 연결한 과정

보고서는 단순히 “어느 지역이 좋다”는 지도를 만드는 데서 멈추지 않았다. 먼저 토양·기후·생산량 자료의 단위와 공간 기준을 맞춘 뒤, 작물별로 생산량 분포와 환경 변수의 관계를 확인했다. Levene 검정은 집단 간 분산이 같은지 살펴보기 위한 전제 점검이고, ANOVA와 Chi-square는 어떤 환경 조건이 작물별 생산량 또는 적합도 구분과 함께 움직이는지 좁히는 역할을 했다. 이 단계를 먼저 둔 덕분에 모든 토양·기후 컬럼을 한꺼번에 모델에 넣는 방식보다, 작물별 설명이 가능한 후보 변수를 가지고 모델을 비교할 수 있었다.

분류 경로에서는 평균 생산량 이상 여부를 적합 기준으로 이진화했고, 회귀 경로에서는 생산량 자체를 별도로 다뤘다. 이것은 ‘적합한 지역인가’와 ‘얼마나 생산될 수 있는가’가 같은 질문이 아니라는 판단이다. 보고서의 모델 선택 결과도 그 차이를 보여 준다. 마는 SVM, 복분자딸기·생표고·더덕·오갈피는 Random Forest, 밤·도라지는 Logistic Regression이 상대적으로 적합했다. 하나의 알고리즘을 모든 작물의 정답처럼 쓰지 않고, 변수 구조와 성능을 함께 보고 선택했다.

최종 구상은 GeoPandas 기반 적지 히트맵과 `적합·보통·부적합`의 세 단계 등급이다. 임업인이 모델의 확률값이나 토양 화학성 컬럼을 직접 읽지 않아도, 후보 지역을 먼저 찾고 근거 변수를 뒤이어 확인할 수 있게 하려는 인터페이스다. 즉 모델의 역할은 재배 결정을 자동으로 대신하는 것이 아니라, 현장 검토를 시작할 후보를 줄이는 데 있다. 밤 AUC 0.94, 마 0.91, 복분자 0.84는 보관된 ROC 산출물에서 확인한 분류 성능이며, 실제 재배 성공률을 뜻하지는 않는다.
